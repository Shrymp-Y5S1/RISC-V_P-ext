# T5/T6 推进记录（2026-04-08）

## 1. 本轮目标

- 打通 T5（actual 导出 + diff）到 T6（归因摘要）的可执行链路。
- 将“手工整理 raw 日志”降为最小成本，避免流程卡住。

## 2. 已完成项

1. 新增 ISS 输入导出脚本：`scripts/export_iss_cases.py`
   - 可导出执行子集 json/csv。
   - 可直接生成 raw 模板 csv（`case_id,actual_rd,actual_vxsat`）。

2. 新增 raw 转 actual 脚本：`scripts/build_actual_from_raw.py`
   - 支持 json/csv/txt。
   - 支持两类文本格式：
     - `C0000001,0x12345678,1`
     - `case_id=C0000001 rd=0x12345678 vxsat=1`

3. 新增 diff 归因摘要脚本：`scripts/summarize_diff.py`
   - 输出总通过率、归因计数、失败指令分布与前 N 条失败样例。

4. 新增 qemu 执行脚本：`scripts/run_qemu_cases.py`
   - 自动将 case 编译为最小 ELF，调用 `qemu-riscv64-static` 执行。
   - 自动输出 `raw csv` + `actual json`，并记录 `status`（如 `ILLEGAL_INSN`）。

5. 一键流程已接入新脚本：
   - `scripts/wsl_run_minset.sh`
   - `scripts/run_week1.ps1`
   - `scripts/wsl_qemu_first20.sh`

6. first20 执行包已生成：
   - `cases/minset_first20_for_iss.json`
   - `cases/minset_first20_for_iss.csv`
   - `actual/minset_first20_raw_template.csv`
   - `expected/minset_first20_for_iss.expected.json`

7. qemu first20 实测已完成：
   - `actual/minset_first20_qemu_raw.csv`
   - `actual/minset_first20_qemu.actual.json`
   - `reports/diff-minset-first20-qemu.csv`
   - `reports/diff-minset-first20-qemu-summary.md`
   - `reports/smoke-qemu-first20-wsl2-20260408.md`
   - 结果：20/20 `ILLEGAL_INSN`，差分 `pass=0 fail=20`
   - 最新回归时间：2026-04-08 08:48（本地）

## 3. 当前阻塞点

- 当前 WSL2 qemu-user（8.2.2）对本 P 子集执行即 `ILLEGAL_INSN`。
- 因此 qemu 路径可用于“阻塞证据”与 T6 归因，但不能产出可用 `actual_rd`。
- 需要切换到支持 P 执行的 ISS/DUT 才能获得真实 T5 通过率。

## 4. 你只需执行的最小动作

1. 若继续走 qemu 路径（用于证据归档），直接运行：

```bash
bash scripts/wsl_qemu_first20.sh
```

2. 若要拿到真实 `actual_rd`，需要在支持 P 的 ISS/DUT 上填写：
   - `actual/minset_first20_raw_template.csv`
   - 每行填 `actual_rd`，可选填 `actual_vxsat`。

3. 然后运行：

```bash
python3 scripts/build_actual_from_raw.py --raw actual/minset_first20_raw_template.csv --out actual/minset_first20.actual.json
python3 scripts/diff.py --expected expected/minset_first20_for_iss.expected.json --actual actual/minset_first20.actual.json --out reports/diff-minset-first20.csv
python3 scripts/summarize_diff.py --diff reports/diff-minset-first20.csv --out reports/diff-minset-first20-summary.md
```

## 5. 预期产物

- `reports/diff-minset-first20.csv`
- `reports/diff-minset-first20-summary.md`
- `reports/error-attribution.md`（按摘要逐条回填）

## 6. QEMU 路径结论

- 现有 qemu-user 路径已验证为当前阶段阻塞点（`ILLEGAL_INSN`）。
- 建议将 qemu 路径保留为回归探针，同时并行接入支持 P 的 ISS/DUT 执行链路。
