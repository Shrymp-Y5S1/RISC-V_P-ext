# P扩展后续重点调研方向（v0.20草案）

> 目标：围绕“risc-v标准里公开的DSP扩展指令集内容及架构”，明确哪些内容最值得深入，支撑组内后续立项与实现路线。

## 1. 选题原则（为什么是这几块）

建议按三条标准筛选：

- 业务收益：对典型DSP内核（滤波、卷积、音频前后处理、控制环）加速收益是否明显。
- 实现风险：微架构复杂度、工具链可达性、验证成本是否可控。
- 草案敏感度：该部分是否容易受规范演进影响，是否需要提前做变更缓冲。

## 2. 建议优先级总览

| 优先级 | 方向 | 为什么值得展开 | 预期价值 |
| --- | --- | --- | --- |
| P0 | 高价值指令子集语义闭环 | 直接决定性能收益，最快形成可展示结果 | 快速证明P扩展“有用” |
| P0 | RV32双寄存器机制与RV64统一抽象 | 实现难点和踩坑高发区 | 降低架构/实现返工 |
| P0 | 编译器与汇编器可达性 | 没有工具链，指令价值无法落地 | 打通从代码到指令的链路 |
| P1 | 微架构代价与时序风险评估 | 决定是否能上产品线 | 明确面积/频率/功耗边界 |
| P1 | 验证体系与一致性策略 | 草案阶段最怕“改一处炸一片” | 建立可演进验证框架 |
| P1/P2 | 标准演进跟踪与边界管理 | 规范仍在发展，易触发变更成本 | 降低后续改版冲击 |
| P1/P2 | 全球RISC-V DSP扩展厂商与生态图谱 | 立项依赖生态成熟度与供应链可得性 | 降低选型与合作风险 |

## 3. 七大重点方向（建议展开内容）

## 3.1 高价值指令子集语义闭环（P0）

### 为什么值得

- 这是最容易体现业务收益的一块。
- 可快速形成“性能对比 + 语义正确性”双证据。

### 建议优先子集

- 饱和加减（Saturating Add/Sub）。
- 移位+舍入（Saturating/Rounding Shift）。
- Q-format乘法/乘加（MULQ/MULQR/MQACC相关）。
- PM2/PM4横向乘加（2ADD/4ADD链）。

### 关键调研问题

- 指令语义在边界输入（极值、符号翻转、溢出）下是否清晰且一致。
- 与标量等价实现相比，理论与实测收益差距多大。
- 哪些子集最适合作为第一版实现范围。

### 预期产出

- 子集语义说明表。
- 边界用例矩阵。
- 基准内核加速结果（至少给出1组代表场景）。

## 3.2 RV32双寄存器机制与RV64统一抽象（P0）

### 为什么值得

- 这是P扩展实现复杂度最高的结构性问题。
- 处理不好会在译码、寄存器读写、验证中反复返工。

### 关键调研问题

- rd+/rs1+/rs2+映射规则在所有双宽形式下是否可统一建模。
- RV32寄存器对语义如何与RV64单寄存器语义对齐。
- x0及配对场景的特殊规则如何在实现中固化。

### 预期产出

- 统一语义中间层设计草案。
- RV32/RV64映射规则清单。
- 高风险场景清单（异常输入、重叠寄存器、边界后缀组合）。

## 3.3 编译器与汇编器可达性（P0）

### 为什么值得

- 指令“可实现”不等于“可使用”。
- 若无法稳定从C/C++触发关键指令，工程价值会被大幅削弱。

### 关键调研问题

- 哪些模式可由自动指令选择稳定触发，哪些需内建函数/内联汇编。
- 双寄存器形式对寄存器分配器的影响有多大。
- 命名与后缀复杂度是否会引入工具链易错点。

### 预期产出

- 最小可用工具链方案（可生成目标指令）。
- 指令触发策略文档（自动匹配 vs 显式调用）。
- 一组可复现的编译输出样例。

## 3.4 微架构代价与时序风险评估（P1）

### 为什么值得

- 决定P扩展是否能在目标频点/面积预算下落地。
- 尤其是饱和、舍入、横向归约类逻辑可能拉长关键路径。

### 关键调研问题

- 哪些指令最可能成为关键路径热点。
- 组合实现与分段流水实现的代价差异。
- 性能提升是否覆盖面积/功耗成本。

### 预期产出

- 面积-频率-性能三维评估表。
- 关键路径热点列表。
- 两种以上实现策略对比建议。

## 3.5 验证体系与一致性策略（P1）

### 为什么值得

- 草案演进期最怕“语义改动导致回归爆炸”。
- 验证策略决定后续维护成本。

### 关键调研问题

- 如何把“语义验证”和“编码验证”解耦。
- 如何构建随机测试 + 定向边界测试组合。
- 如何与ISS/参考模型建立自动对照闭环。

### 预期产出

- 分层验证方案（语义层、编码层、回归层）。
- 边界样例库与随机样例策略。
- 回归准入标准（通过率、覆盖率、差异定位流程）。

## 3.6 标准演进跟踪与边界管理（P1/P2）

### 为什么值得

- 0.20-draft仍处Development状态，后续改动几率高。
- 提前做跟踪机制可显著降低规范变更冲击。

### 关键调研问题

- 哪些章节/指令族在后续版本最可能调整。
- 变更对语义层、编码层、工具链层影响如何分级。
- 与其他扩展（如V）边界怎样在架构文档中描述清楚。

### 预期产出

- 版本差异跟踪模板。
- 变更影响分级表（低/中/高）。
- 对应应对动作（无需改动/局部修复/架构调整）。

## 3.7 全球RISC-V DSP扩展厂商与生态图谱（P1/P2）

### 为什么值得

- 仅看指令语义无法判断商业可落地性。
- 不同厂商对“DSP扩展”的定义差异大，需提前做证据分层。

### 关键调研问题

- 哪些公司公开明确宣称支持RISC-V DSP/P扩展或等价扩展。
- 哪些公司仅有向量/AI能力，尚不足以判定为DSP扩展。
- 各公司工具链/SDK/验证支持成熟度如何。

### 预期产出

- 厂商分级清单（明确支持/间接证据/待核验）。
- 证据链接库（官网、手册、SDK、开源仓库）。
- 合作与选型建议（短期可接入 vs 中长期跟踪）。

## 4. 推荐推进顺序

建议先做 P0 的 3 块，再做 P1：

1. 高价值指令子集语义闭环。
2. RV32双寄存器与RV64统一抽象。
3. 编译器与汇编器可达性。
4. 微架构代价评估。
5. 验证体系完善。
6. 标准演进跟踪常态化。
7. 全球厂商与生态图谱季度更新。

## 5. 组会可直接引用的总结

当前最值得展开的不是“把全部指令逐条复述”，而是围绕高价值子集、跨XLEN统一抽象、工具链可达性三条主线先形成闭环证据；在此基础上，再用微架构评估与验证体系把方案从“可讲”推进到“可落地”，并通过版本跟踪机制应对草案演进风险，同时用全球厂商与生态图谱降低选型与合作不确定性。